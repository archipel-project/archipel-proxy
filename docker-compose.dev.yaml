version: "3"

services:
  minecraft:
    image: itzg/minecraft-server
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      MOTD: "A §l§cMinecraft§r §nserver"
    tty: true
    volumes:
      - minecraft_data:/data

volumes:
  minecraft_data:
